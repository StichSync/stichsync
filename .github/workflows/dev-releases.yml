on: 
  push:
    branches: 
      - develop

name: Development release
run-name: Dev build triggered by @${{ github.actor }}

env:
  environment: dev

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            for: android
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v1
      - name: Setup GitVersion
        uses: gittools/actions/gitversion/setup@v1.1.1
        with:
          versionSpec: '5.x'
      - name: Determine version
        uses: gittools/actions/gitversion/execute@v1.1.1
        id: version_step
        with:
          useConfigFile: true
          configFilePath: '.github/GitVersion.yml'
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'oracle'
          java-version: '17'
      - name: Setup flutter
        uses: subosito/flutter-action@v2
        env:
          ACTIONS_ALLOW_UNSECURE_COMMANDS: true
      - name: Install app dependencies
        run: flutter pub get
      - name: Analyze app code
        run: flutter analyze --no-fatal-infos
      - name: Flutter doctor
        run: flutter doctor
      - name: Build andorid apk
        if: ${{ matrix.for }} == 'android'
        run: flutter build apk --release --split-debug-info=build/app/outputs/symbols
      - name: Update artifact
        uses: actions/upload-artifact@v2
        with:
          name: stichsync-v${{ steps.version_step.outputs.majorMinorPatch }}-${{ env.environment }}-${{ matrix.for }}
          path: "build/app/outputs/apk/release/app-release.apk"
    outputs:
      version_output: ${{ steps.version_step.outputs.majorMinorPatch }}
  deploy:
    needs: build
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            for: android
    runs-on: ${{ matrix.os }}
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v2
        with:
          name: stichsync-v${{ needs.build.outputs.version_output }}-${{ env.environment }}-${{ matrix.for }}
      - name: Change package name
        if: ${{ matrix.for }} == 'android'
        run: mv app-release.apk stichsync-v${{ needs.build.outputs.version_output }}-${{ env.environment }}-${{ matrix.for }}.apk
      - name: Upload release
        uses: ncipollo/release-action@v1
        with:
          tag: v${{ needs.build.outputs.version_output }}-${{ env.environment }}
          artifacts: "stichsync*-${{ env.environment }}-*"
          name: stichsync-v${{ needs.build.outputs.version_output }}-${{ env.environment }}
          token: ${{ secrets.TOKEN }}