import 'package:flutter/material.dart';

class ThemeConfig {
  // colors
  static const Brightness appBrightness = Brightness.dark;

  // lets have a full color theme there in the future:
  // static const ThemeData appColorTheme = ...;

  // animations
  static const Duration buttonDuration = Duration(milliseconds: 150);
  static const Curve buttonCurve = Curves.easeInOut;
}
