import 'package:flutter/material.dart';

// This page will display users saved crochets and needed ingredients.
// User can mark any crochet as "in progress", even from the feed.
// The representation of that action in db
// is that we add the savedCrochet record in db and mark it as inProgress
// There also will be a "purchase ingredients feature there"
class Saved extends StatelessWidget {
  const Saved({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
